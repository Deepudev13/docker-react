sudo: required
services:
  - docker

before_install:
  - docker build -t loudcloudorg/docker-react -f Dockerfile.dev .

script:
  - docker run loudcloudorg/docker-react npm run test -- --coverage


  deploy:
    provider: docker-react
    username: "docker-react\dsakfidasjflkasdhfljkasdhflkjas"
    password: "Pass@123Pass@123"
    on:
      branch: master
    